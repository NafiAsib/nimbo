---
import { navLinks } from '@/data'
---

<nav>
  <ul class='flex gap-x-8'>
    {
      navLinks.map((link) => (
        <li
          class='text-lg hover-underline'
          aria-label={`Opens ${link.name} of Nafi`}
        >
          <a
            href={link.url}
            target={link.url.includes('http') ? '_blank' : '_self'}
          >
            {link.name}
          </a>
        </li>
      ))
    }
  </ul>
</nav>
